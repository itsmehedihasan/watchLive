watchlive.exe
watchlive.exe~
watchlive-native.exe
# Anchored to the repo root so it ignores a root-built `watchlive` binary
# WITHOUT also ignoring the cmd/watchlive/ package directory.
/watchlive
# mpv.exe is fetched by fetch-mpv.ps1, not committed (~40-80 MB, GPL player build).
mpv/
mpv.exe
list.m3u
list.txt.bak
list.m3u.bak
health.json
health.json.tmp
# Stray dev artifacts: a scratch playlist copy and captured stdout/stderr.
# (seed.m3u is intentionally tracked — it is //go:embed-ed into the binary.)
wc.m3u
run.err
run.out
catalog.db
catalog.db-wal
catalog.db-shm
# Throwaway -playlist session DB (reset on every run).
playlist.db
playlist.db-wal
playlist.db-shm
recordings/
# ClearKey DRM pairs — local convenience cache, never committed.
keys.json
keys.json.tmp
# Embedded ffmpeg is fetched by build.ps1, not committed (keep bin/.gitkeep).
internal/ffmpeg/bin/ffmpeg.exe
internal/ffmpeg/bin/ffmpeg
libmpv/*
# libmpv shared library (~117 MB) — fetched by fetch-mpv.ps1, never committed.
libmpv-2.dll